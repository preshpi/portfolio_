import blockContent from './blockContent'
import allproject from './allproject'
import skillData from './skillData'
import resume from './resume'

export const schemaTypes = [blockContent, allproject, skillData, resume]
